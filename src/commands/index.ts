import * as ping from "./ping";
import * as whitelist from "./whitelist";
import * as peruna from "./peruna";
//import * as esta from "./ban";

export const commands = {
  whitelist,
  ping,
  peruna,
};

// Konsoli roskat
console.log(commands);